apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "atm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
