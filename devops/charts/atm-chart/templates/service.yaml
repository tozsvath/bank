apiVersion: v1
kind: Service
metadata:
  name: {{ include "atm-chart.fullname" . }}
  labels:
    {{- include "atm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atm-chart.selectorLabels" . | nindent 4 }}
